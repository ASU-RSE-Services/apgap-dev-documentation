version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"

  jobs:
    install:
      - uv sync
    build:
      html:
        - uv run zensical build
    post_build:
      - mkdir -p $READTHEDOCS_OUTPUT/html/
      - cp --recursive site/* $READTHEDOCS_OUTPUT/html/